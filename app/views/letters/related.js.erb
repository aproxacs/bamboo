$(".letter-<%= @letter.id %> .related-letters").hide().html(
  "<%= escape_javascript(render partial: 'related_letters', locals: {related_letters: @related_letters, letter: @letter}) %>"
).fadeIn();

